# Tasks (web fixture)

A single-file web app with no build step. It exists so evergif has a realistic
web project to record with Playwright.

Serve it with `python3 -m http.server 8000 --directory examples/webapp` and
open <http://localhost:8000>.
